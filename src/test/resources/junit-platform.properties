# Enables JUnit 5 parallel test execution (classes and methods run concurrently). Safe because
# every API test creates/cleans up its own data and every UI test gets its own Playwright
# BrowserContext/Page (see PlaywrightExtension) - no test depends on another test's state.
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.mode.default=same_thread
junit.jupiter.execution.parallel.mode.classes.default=concurrent
junit.jupiter.execution.parallel.config.strategy=dynamic
junit.jupiter.execution.parallel.config.dynamic.factor=2
